// ============================================================
// Pack pickups — the SHARED, committed record of who has
// collected their packs.   name -> number of packs picked up.
// ------------------------------------------------------------
// You don't edit this by hand. On the leaderboard, tick people
// off as they collect (saved instantly in your browser), then
// hit "Copy pickup list to commit" and paste the result here,
// or just tell Claude to save it. Deploy to publish for everyone.
// ============================================================

const COLLECTED = {
};
